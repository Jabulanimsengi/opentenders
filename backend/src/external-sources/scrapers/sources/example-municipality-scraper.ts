import { StaticHtmlScraper } from '../static-html-scraper';

export class ExampleMunicipalityScraper extends StaticHtmlScraper {
  // Add source-specific selectors or row cleanup here when a municipality needs it.
}
