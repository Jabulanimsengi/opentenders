import { StaticHtmlScraper } from '../static-html-scraper';

export class ExampleUniversityScraper extends StaticHtmlScraper {
  // Add university-specific parsing here when the source structure is known.
}
